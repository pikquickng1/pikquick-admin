# Settings & Roles Feature

This feature provides settings and roles management functionality for the PikQuick Admin Dashboard.

## Structure

- `components/` - React components for settings UI
- `api/` - API integration layer
- `hooks/` - Custom React hooks
- `types/` - TypeScript type definitions

## Components

- `SettingsOverview.tsx` - Main settings dashboard with navigation cards
